export const getRoomName = (prefix: string, id: string) => `${prefix}-${id}`

export const getUserRoomName = (userId: string) => `user-${userId}`

export const getGroupRoomName = (groupId: string) => `group-${groupId}`
